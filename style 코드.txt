:root {
    --bg: #000;
    --text: #fff;
    --accent-purple: #a855f7;
    --accent-cyan: #06b6d4;
    --accent-orange: #f97316;
    --accent-green: #22c55e;
    --font-heading: 'Orbitron', sans-serif;
    --font-body: 'Inter', sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    background-color: var(--bg);
    color: var(--text);
    font-family: var(--font-body);
    overflow: hidden; /* No scroll in SPA */
}

#webgl-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
}

#app {
    position: relative;
    z-index: 2;
    width: 100%;
    height: 100vh;
}

/* Home Navigation */
#home-nav {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0 10%;
    transition: opacity 1s ease, transform 1s ease;
}

.home-menu {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.menu-item {
    font-family: var(--font-heading);
    font-size: 4rem;
    font-weight: 900;
    color: transparent;
    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.3);
    text-decoration: none;
    text-transform: uppercase;
    transition: all 0.4s ease;
}

.menu-item:hover {
    color: #fff;
    -webkit-text-stroke: 1px #fff;
    padding-left: 2rem;
    text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
}

.home-title {
    margin-top: 3rem;
}

.home-title h1 {
    font-size: 1rem;
    letter-spacing: 0.5rem;
    opacity: 0.5;
}

/* Content Overlay */
#content-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    padding: 0 10%;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

.view-section {
    display: none;
    width: 100%;
}

.view-section.active {
    display: block;
}

.left-align { text-align: left; justify-content: flex-start; }
.right-align { text-align: right; justify-content: flex-end; margin-left: auto; }

.view-title {
    font-family: var(--font-heading);
    font-size: 3.5rem;
    margin-bottom: 2rem;
    text-transform: uppercase;
}

.glass-container {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    padding: 2.5rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    max-width: 600px;
}

.back-btn {
    position: absolute;
    top: 50px;
    left: 10%;
    background: none;
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #fff;
    padding: 0.5rem 1.5rem;
    cursor: pointer;
    font-family: var(--font-heading);
    font-size: 0.8rem;
    letter-spacing: 2px;
    z-index: 100;
}

/* Portfolio Stats */
.stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    max-width: 800px;
}

.stat-item {
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.stat-number {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    display: block;
    color: var(--accent-cyan);
}

/* Guestbook */
.guestbook-form {
    max-width: 450px;
    margin-top: 2rem;
    background: rgba(0, 0, 0, 0.5);
    padding: 2rem;
    border: 1px solid var(--accent-green);
    display: inline-block;
}

input, textarea {
    width: 100%;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1rem;
    color: #fff;
    margin-bottom: 1rem;
}

.form-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.upload-btn, .submit-btn {
    padding: 0.8rem 1.5rem;
    cursor: pointer;
    font-weight: bold;
    font-size: 0.8rem;
}

.upload-btn { border: 1px solid #fff; }
.submit-btn { background: var(--accent-green); border: none; color: #000; }

/* Loader */
#loader {
    position: fixed;
    z-index: 1000;
    background: #000;
    width: 100%; height: 100%;
    display: flex; justify-content: center; align-items: center;
}

.loader-bar {
    width: 0%; height: 2px; background: var(--accent-purple); margin-top: 20px;
}

/* Neon Styles */
.neon-purple { border-left: 4px solid var(--accent-purple); padding-left: 1rem; }
.neon-cyan { border-left: 4px solid var(--accent-cyan); padding-left: 1rem; }

@media (max-width: 768px) {
    .menu-item { font-size: 2.5rem; }
    .stats-grid { grid-template-columns: 1fr; }
}
